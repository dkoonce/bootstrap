#Install git credential helper for GNOME Keyring
sudo apt install -y git libgnome-keyring-dev
cp -rp /usr/share/doc/git/contrib/credential/gnome-keyring $HOME
cd $HOME/gnome-keyring
make
sudo install -o root -m 0755 git-credential-gnome-keyring /usr/local/bin/
cd ..
rm -rf gnome-keyring
